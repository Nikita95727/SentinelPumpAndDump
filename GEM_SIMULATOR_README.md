# 💎 Gem Simulator - Симулятор стратегии выявления самородков

## 🎯 Цель

Реалистичный симулятор для тестирования гипотезы о раннем выявлении самородков:
1. **Мониторит токены** без входа (после honeypot check)
2. **Выявляет самородки** на ранней стадии по индикаторам
3. **Входит только в подтвержденные самородки** (multiplier ≥ 2.0x + gem score ≥ 0.5)
4. **Выходит на обратном импульсе** (когда импульс замедляется)

## 🚀 Запуск

### Режим разработки (с hot reload):
```bash
npm run gem:sim
```

### Продакшн режим (скомпилированный):
```bash
npm run build
npm run gem:sim:build
```

### Через PM2 (для 24/7 работы):
```bash
# Создайте ecosystem.config.js с gem-simulator-index.ts
pm2 start ecosystem.config.js --name gem-simulator
```

## 📊 Что логируется

### Детальное неблокирующее логирование:

1. **Мониторинг токенов:**
   - Начало мониторинга (initialPrice, initialVolume, initialHolders)
   - Прогресс каждые 30 секунд (multiplier, gemScore, timeElapsed)

2. **Обнаружение самородков:**
   - Все индикаторы: entryMultiplier, gemScore, priceMomentum, volumeGrowth, holderGrowth, marketCapGrowth
   - Время обнаружения от начала мониторинга

3. **Открытие позиций:**
   - Entry multiplier, invested SOL, entry price, entry slippage
   - Все индикаторы на момент входа

4. **Мониторинг позиций:**
   - Каждые 5 секунд: multiplier, peak, dropFromPeak, priceMomentum, timeHeld

5. **Закрытие позиций:**
   - Reason (timeout, momentum_reversal, peak_drop, min_profit_momentum)
   - Entry multiplier, exit multiplier, profit, exit slippage, timeHeld

6. **Статистика (каждые 60 секунд):**
   - Total monitored, gems detected, positions opened/closed
   - Win rate, avg entry/exit multipliers, total profit

## 📈 Индикаторы самородка

### Критерии входа:
- **Multiplier ≥ 2.0x** (токен уже показал рост)
- **Gem Score ≥ 0.5** (комбинированный индикатор)
- **Price Momentum ≥ 0.05x/сек** (быстрый рост)
- **Volume Growth ≥ 50%** (объем растет)
- **Нет резких падений** (>10% от пика)

### Критерии выхода:
- **Timeout**: 45 секунд
- **Momentum Reversal**: price momentum < 0.02x/сек (замедление роста)
- **Peak Drop**: падение от пика на 15-20%
- **Min Profit Momentum**: multiplier ≥ 2.5x и momentum < 0.05x/сек

## 🔍 Анализ результатов

После суток работы соберите статистику:

1. **Откройте логи:**
   ```bash
   tail -f logs/YYYY-MM-DD.log
   ```

2. **Ищите закономерности:**
   - Какие индикаторы коррелируют с прибыльностью?
   - Какие gem scores дают лучшие результаты?
   - Какие price momentum значения оптимальны?
   - Какой средний hold time для прибыльных сделок?

3. **Анализируйте распределения:**
   - Gem Score Distribution
   - Price Momentum Distribution
   - Entry/Exit Multiplier Distribution

## ⚙️ Настройка

Все параметры в `src/gem-tracker.ts`:
- `MONITORING_INTERVAL_MS`: интервал проверки (по умолчанию 5 секунд)
- `MAX_MONITORING_TIME_MS`: максимальное время мониторинга (по умолчанию 2 минуты)
- `MIN_GEM_SCORE`: минимальный gem score (по умолчанию 0.5)
- `MIN_PRICE_MOMENTUM`: минимальная скорость роста (по умолчанию 0.05x/сек)
- `MIN_VOLUME_GROWTH`: минимальный рост объема (по умолчанию 50%)
- `MIN_ENTRY_MULTIPLIER`: минимальный multiplier для входа (по умолчанию 2.0x)

## 📝 Примеры логов

### Обнаружение самородка:
```
💎 GEM DETECTED: 7xK9m2... | entryMultiplier=2.345x, gemScore=0.678, priceMomentum=0.0823x/sec, volumeGrowth=125.4%, holderGrowth=45.2%, marketCapGrowth=89.1%, timeElapsed=12.3s
```

### Открытие позиции:
```
✅ GEM SIM: Position opened 7xK9m2... | entryMultiplier=2.345x, invested=0.004000 SOL, entryPrice=0.00001234 SOL, entrySlippage=18.5%, gemScore=0.678, priceMomentum=0.0823x/sec, duration=234ms
```

### Мониторинг позиции:
```
📊 GEM SIM: Position status 7xK9m2... | multiplier=3.456x, peak=4.123x, dropFromPeak=16.2%, priceMomentum=0.0123x/sec, timeHeld=23.4s
```

### Закрытие позиции:
```
✅ GEM SIM: Position closed 7xK9m2... | reason=momentum_reversal, entryMultiplier=2.345x, exitMultiplier=3.456x, profit=+0.002345 SOL (+15.23%), exitSlippage=22.1%, timeHeld=23.4s, deposit=0.100000 → 0.102345 SOL
```

## 🎯 Ожидаемые результаты

- **Частота сделок**: 5-10 в день (вместо 72-96)
- **Качество**: multiplier при входе ≥ 2.0x
- **Slippage**: 15-25% (меньше, т.к. больше ликвидности)
- **Математическое ожидание**: +0.001270 SOL на сделку

## ⚠️ Важно

- Все логи пишутся **неблокирующим** способом (через `setImmediate`)
- Симулятор использует **реальные данные** из blockchain
- Все задержки **реалистичные** (как в реальной торговле)
- Статистика обновляется каждые 60 секунд

## 📊 Финальная статистика

При остановке (Ctrl+C) выводится полная статистика:
- Total Monitored
- Gems Detected (%)
- Positions Opened/Closed
- Win Rate
- Avg Entry/Exit Multipliers
- Avg Hold Time
- Total Profit
- Max Drawdown

