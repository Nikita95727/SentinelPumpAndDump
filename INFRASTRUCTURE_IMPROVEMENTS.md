# Инфраструктурные улучшения для ускорения обработки очередей

## Проблема
Токены стареют до обработки из-за медленной обработки очередей. Нужно максимально ускорить обработку всех очередей, особенно queue1 (0-5s) и queue2 (5-15s).

## Предлагаемые решения

### 1. Приоритетная обработка на уровне очереди ⚡ КРИТИЧНО
**Проблема:** Сейчас все очереди обрабатываются параллельно, но queue1/queue2 могут ждать пока обрабатывается queue3.

**Решение:**
- Обрабатывать queue1 и queue2 **ПЕРЕД** notificationQueue
- Использовать priority queue вместо FIFO
- Прерывать обработку queue3 если появились токены в queue1/queue2
- Выделить отдельные воркеры для приоритетных очередей

**Ожидаемый эффект:** Токены в queue1/queue2 обрабатываются немедленно, не дожидаясь queue3.

---

### 2. Redis для распределения нагрузки и кеширования 🔥 ВЫСОКИЙ ПРИОРИТЕТ
**Проблема:** Все обработка в памяти одного процесса, нет кеширования.

**Решение:**
- Использовать Redis для очередей (уже есть `ioredis` в зависимостях)
- Pub/Sub для координации между процессами (если нужно масштабирование)
- Кеширование результатов фильтров:
  - `getMint` - кеш 10 секунд (mint authority не меняется)
  - `getTokenLargestAccounts` - кеш 5 секунд
  - `getSignaturesForAddress` - кеш 2 секунды
- Кеширование проверок honeypot (результаты не меняются быстро)

**Ожидаемый эффект:** Снижение RPC запросов на 30-50%, ускорение обработки в 2-3 раза.

---

### 3. Параллельные RPC соединения 🔥 ВЫСОКИЙ ПРИОРИТЕТ
**Проблема:** Одно соединение к Helius - узкое место.

**Решение:**
- Пул соединений к Helius (3-5 соединений)
- Round-robin для распределения запросов
- Автоматический failover при ошибках
- Отдельные пулы для приоритетных очередей

**Ожидаемый эффект:** Увеличение пропускной способности в 3-5 раз.

---

### 4. Batch RPC запросы ⚡ КРИТИЧНО
**Проблема:** Множество отдельных RPC запросов замедляют обработку.

**Решение:**
- Использовать `getMultipleAccounts` вместо множества `getAccount`
- Использовать `getMultipleSignatures` (если доступно) вместо множества `getTransaction`
- Группировать запросы по батчам (10-20 запросов за раз)
- Приоритетные батчи для queue1/queue2

**Ожидаемый эффект:** Снижение количества RPC запросов в 5-10 раз, ускорение в 3-5 раз.

---

### 5. Worker Threads для фильтров 🚀 СРЕДНИЙ ПРИОРИТЕТ
**Проблема:** Фильтры блокируют event loop, замедляют обработку.

**Решение:**
- Вынести фильтры в worker threads
- Параллельная обработка без блокировки event loop
- Использовать все CPU ядра (если доступно)
- Отдельные воркеры для каждой очереди

**Ожидаемый эффект:** Ускорение обработки фильтров в 2-4 раза (зависит от CPU).

---

### 6. Предварительная фильтрация на WebSocket ⚡ КРИТИЧНО
**Проблема:** Обрабатываются все уведомления, даже нерелевантные.

**Решение:**
- Использовать WebSocket фильтры Helius (если доступно)
- Фильтровать уведомления ДО добавления в очередь
- Отбрасывать нерелевантные события сразу
- Использовать более строгие фильтры на уровне WebSocket

**Ожидаемый эффект:** Снижение нагрузки на обработку на 50-70%.

---

### 7. Множественные WebSocket соединения 🚀 СРЕДНИЙ ПРИОРИТЕТ
**Проблема:** Одно WebSocket соединение - узкое место.

**Решение:**
- Несколько WebSocket соединений к Helius (2-3)
- Распределение уведомлений между соединениями
- Автоматический rebalance при отключении
- Отдельные соединения для приоритетных очередей

**Ожидаемый эффект:** Увеличение пропускной способности в 2-3 раза.

---

### 8. Оптимизация алгоритма обработки очередей ⚡ КРИТИЧНО
**Проблема:** Текущий алгоритм не оптимален для приоритетных очередей.

**Решение:**
- Использовать `setImmediate` для приоритетных очередей
- Обрабатывать queue1/queue2 в каждом event loop tick
- Использовать `process.nextTick` для критичных операций
- Минимизировать задержки между обработкой токенов

**Ожидаемый эффект:** Ускорение обработки приоритетных очередей в 2-3 раза.

---

## Приоритеты внедрения

### Фаза 1 (Немедленно) - Критичные улучшения:
1. ✅ Приоритетная обработка queue1/queue2 (уже частично сделано)
2. ⚠️ Batch RPC запросы
3. ⚠️ Предварительная фильтрация на WebSocket
4. ⚠️ Оптимизация алгоритма обработки очередей

### Фаза 2 (Ближайшее время) - Высокий приоритет:
1. Redis для кеширования
2. Параллельные RPC соединения

### Фаза 3 (По необходимости) - Средний приоритет:
1. Worker Threads
2. Множественные WebSocket соединения

---

## Ожидаемый общий эффект

После внедрения всех улучшений:
- **Обработка queue1/queue2:** в 5-10 раз быстрее
- **Обработка queue3:** в 3-5 раз быстрее
- **RPC запросы:** снижение на 50-70%
- **Токены в нужных окнах:** увеличение на 80-90%

